# Repository notes

## Build and verify

- PlatformIO project; `default_envs` comes from ignored `platformio.local.ini`. The committed example `platformio.local.example.ini` ships `lamp1_ota`; any additional per-lamp envs are local-only.
- **For verification always build exactly ONE focused env. Default command: `just lamp build lamp1_ota`.** Do not run `pio run` without `-e` and do not build multiple envs unless explicitly requested.
- Lamp 1 (`env:lamp1_base` in the example) uses board `d1_mini`, `DEVICE_NAME="GyverLamp1"` (which becomes `AP_SSID` via the default in `src/config.h`), and `CURRENT_LIMIT=3000`.
- Available focused builds from the example: `just lamp build lamp1_usb`, `just lamp build lamp1_ota`. Additional `lampN_*` envs may be defined locally in `platformio.local.ini`; do not assume their names or counts.
- There are no separate tests/lint checks for firmware; verification is the appropriate focused `just lamp build <env>` command.
- Formatting: `.clang-format` (clang-format 14+) encodes the house style — 2-space indent, attached braces, type-aligned `&`/`*`, case indented under `switch`, includes **not** sorted. Run `clang-format -i <file>` to apply, or `clang-format --dry-run -Werror <file>` to check. Do not reformat files unrelated to your change.
- Do not run `just lamp upload <env>` unless explicitly requested to flash a device; `lamp*_ota` uses `espota` with `upload_port` (device IP) from `platformio.local.ini`.

## Simulator commands

- Simulator commands: `just sim run`, `just sim build`, `just sim test`, `just sim catalog`, `just sim render <args>`.
- Build and upload firmware only with `just lamp build <env>` and `just lamp upload <env>`; do not use `just run sim` or `just build sim`.
- Deterministic WASM/Node export and request/schema format are documented in [`sim/README.md`](sim/README.md). Export results reside in ignored `.artifacts/`; the exporter rejects missing or stale WASM identity/artifacts.
- Store local deterministic render request files in ignored `.artifacts/requests/`. Store render output in `.artifacts/sim/` or another `.artifacts/` subdirectory. Reusable shared request fixtures belong in tracked `sim/fixtures/requests/`.

```bash
just sim render --request .artifacts/requests/<name>.json --out .artifacts/sim
```

## LSP / `compile_commands.json`

- The repo keeps LSP working via a `compile_commands.json` in the project root.
- It is generated by PlatformIO: `pio run -e lamp1_ota -t compiledb`.
- `extra_script.py` sets `COMPILATIONDB_INCLUDE_TOOLCHAIN=True` and `COMPILATIONDB_PATH=compile_commands.json` so toolchain/framework headers (Arduino, ESP8266 SDK) are visible to clangd/LSP.
- If you see missing header errors from the LSP, regenerate `compile_commands.json` before investigating code issues.
- `compile_commands.json` and `.pio/` are ignored; do not commit them.

## Firmware shape

- `src/main.cpp` only starts Serial and delegates to global `lamp` (`src/core/lamp.cpp` / `.h`); start there for execution flow.
- `Lamp::setup()` order matters: LED, button, notifications, EEPROM, settings, effects, power, WiFi, OTA, MQTT, time, web, rotation timer.
- `Lamp::loop()` services rotation timer, power/frame rendering, delayed settings persistence, time, button, WiFi, OTA, web, MQTT, state-notifier MQTT publish, profiler, then `yield()`.
- Hardware/network constants live in `src/config.h`; per-lamp `DEVICE_NAME` (which feeds `AP_SSID`) and `CURRENT_LIMIT` are injected by `platformio.local.ini` build flags.
- `FrameRenderer::render()` ticks global/effect parameter limiters before frame scheduling. While a limiter runs, rendering is forced so applied values reach LED output at frame cadence.
- `Led::showLeds()` applies a per-frame proportional brightness envelope from FastLED power estimation and `CURRENT_LIMIT`; FastLED's configured current limiter remains the final safety net.

## Effects and persistence gotchas

- Effect identity is centralized in `src/effect/ids.h` as `Effects::Id`; UI/MQTT names and factory mapping live in `src/effect/catalog.cpp`.
- Only one effect is active at a time. Reuse suitable working state from `src/effect/shared.h` before adding effect-instance fields: those shared variables deliberately conserve ESP RAM across effects. Initialize every shared value an effect owns in `setup()` and never rely on its value from a previous effect.
- Effect catalog lives in `src/effect/catalog.h`; effect implementations live in `src/effect/catalog/` with files named `<effect>.h/.cpp` (no `effect_` prefix); `EFFECT_REGISTRY` drives IDs, names, and `createEffect()`.
- Adding/removing effects must keep `Effects::Id`, `Effects::COUNT`, `EFFECT_REGISTRY`, `Effects::DISPLAY_ORDER`, and `createEffect()` in sync; web and MQTT enumerate `Effects::DISPLAY_COUNT`.
- `EffectController` uses placement-new into storage sized by `Effects::STORAGE_SIZE`; static assert caps max effect object size at 64 bytes.
- `EffectSettings` must remain exactly 3 bytes (`brightness`, `speed`, `scale`); `eeprom_layout.h` has static assert because EEPROM addresses assume this shape.
- `SettingsRepository` stores target brightness and effect settings; `EffectController` owns separately applied values used for rendering. Do not publish state notifier events for per-frame applied-value changes.
- EEPROM layout is append-only/versioned in `src/storage/eeprom_layout.h`.
- Settings changes persist after ~30s via `SettingsRepository::tick()`; direct effect changes may not hit EEPROM immediately.

## Network/UI behavior

- Active control interfaces: web UI (`src/network/web_service.cpp`), MQTT/Home Assistant (`src/network/mqtt_service.cpp`, built on the `HaMqttEntities` library), and optional UDP (`src/network/upd_service.cpp`, compiled in only when `USE_UDP` is defined — disabled by default, not present in `platformio.local.example.ini`).
- MQTT command handlers update only affected HA entity targets immediately. `HaMqttEntities` publishes dirty entities in its controller loop; `MqttService` retains full state synchronization on reconnect and performs an additional full refresh every 30 seconds. Telemetry remains on its separate one-minute timer.
- `MqttService` lifecycle states are `Disabled`, `WaitingForWifi`, `DisconnectBarrier`, `RetryWait`, `ConnectPrepare`, `Connecting`, `Online`, and `ConfigError`. Its public control API is deferred: `requestApply(MqttConfig)`, `requestEnabled(bool)`, `requestRestart()`. Network operations occur only in `tick()`; status/state are cached for UI reads.
- `PubSubClient`/`HaMqttEntities` connect is still synchronous and may block about 2 seconds. Do not claim MQTT is fully non-blocking and do not fork `HaMqttEntities` for this behavior. `HAMQTT.begin()` and entity registration happen once per boot. Before every connect, use the no-fork barrier: disconnect or abort transport, one `HAMQTT.loop()` pulse, then a fresh prepare pulse before connecting.
- MQTT requires STA; WiFi loss aborts its transport. Broker failures back off from 5 to 60 seconds and do not permanently disable it. Web `Save and apply` calls `requestApply` only after `writeMqttConfig()` successfully commits credentials. Web `Enabled`/`Restart MQTT` are runtime-only policy controls; WiFi settings still persist then restart ESP.
- The web UI is built with the `SettingsAsync` library (GyverLibs/Settings): `WebService::settingsBuilder()` renders forms via `sets::Builder`, and `settingsUpdate()` consumes submits via `sets::Updater`. There are no hand-rolled route registrations or HTML chunk streaming helpers.
- WiFi and MQTT credentials are captured through the web UI (not a captive portal) and stored in fixed-size buffers from `src/network/wifi_config.h` and `src/network/mqtt_config.h`; use `strlcpy` and keep EEPROM size impact in mind.
- `WifiService` (`src/network/wifi_service.cpp`) drives STA/AP directly with the ESP8266 WiFi API (no WiFiManager). Connection is non-blocking and `WifiService` owns retries (`WiFi.setAutoReconnect(false)`). With saved STA credentials it boots in `WIFI_STA`, starts a 60-second connection campaign, and retries failed attempts after 5 seconds; fast-fail disconnect events avoid waiting for an expired attempt. If no STA connection succeeds in that campaign, it starts one fallback setup AP in `WIFI_AP_STA` at `AP_IP` / `AP_SSID`; success stops it. The fallback AP stops after 5 continuous minutes without stations and does not reopen in the same campaign. With no saved SSID it immediately starts a persistent setup AP in `WIFI_AP`; it has no idle timeout. WiFi web settings currently persist then restart ESP; there is no live WiFi apply API.
- `OtaService` is compiled as PlatformIO/ArduinoOTA only with `USE_OTA`; otherwise its `init()`/`tick()` are no-op and Web UI has no OTA section. Only in OTA builds it receives `EepromStore` and reads/writes the reserved EEPROM policy byte for the persisted disabled-by-default PlatformIO OTA policy; listener runtime state is not persisted.
- The OTA Web UI controls PlatformIO OTA only: status, enable/disable, and deferred listener restart. SettingsAsync HTTP `/ota` remains independent. Listener runs only while policy is enabled and STA is connected; loss of WiFi stops it, reconnect starts it again. Deferred control requests do not interrupt an active update; OTA progress notifications apply only to PlatformIO OTA and display updates are throttled.

## Control Pad (ESP-NOW)

- The optional lamp feature is `USE_CONTROL_PAD`; define it **only** in the intended ESP32/ESP32-S3 device environment. `src/config.h` deliberately errors when it is defined for a non-Arduino-ESP32 target; ESP8266 builds must remain feature-free. Do not put the flag in shared `[env]` flags.
- The panel is a separate nested PlatformIO project under `control-pad/`. Its local `platformio.local.ini` is ignored and contains the pairing key; do not read, print, overwrite, commit, or assume the names of its local profiles. New profiles are provisioned with `just panel provision <name>`.
- Focused panel commands: `just panel build diag`, `just panel build input`, and `just panel build <profile>`. `diag` is raw GPIO/EC11 diagnostics; `input` validates production input UX over Serial but is radio/credential-free. Production profiles build the radio source. Run `sh control-pad/tests/run.sh` for panel host tests.
- **Upload safety:** `just panel upload <profile> <serial-port>` requires an explicit port and must not auto-select a device. Example: `just panel upload panel1 /dev/cu.usbmodem2101`. Never upload unless the user explicitly requests it.
- The shared protocol is `lib/ControlPadProtocol/`, namespace `ControlPadProtocol`. Do not recreate its wire structs/codecs or rename it back. It owns pairing/probe messages, `Command`/`CommandAck`, KDF/HMAC, canonical binding codecs and host KATs. Run `sh lib/ControlPadProtocol/tests/run.sh` after protocol changes.
- Lamp integration is `src/network/control_pad_service.{h,cpp}` plus conditional `Lamp`/`WebService` wiring. ESP-NOW callbacks may only length-check/copy into the bounded queue; HMAC, peer changes, EEPROM writes, controller mutations and ACK sends run in `ControlPadService::tick()`. Pairing candidate keys stay RAM-only and binding commits before `ConfirmAck`; duplicate confirm re-ACKs without EEPROM rewrite. Never log artifact/key/MAC/tag data in UI or Serial.
- Panel radio lives under `control-pad/src/radio/` and `control-pad/include/panel_radio_*.h`. It does not join the router: it scans country-legal channels for pairing/recovery, persists only its 16-byte lamp binding in Preferences, retries one exact command at 150 ms and drops it after 500 ms. Failed commands are never replayed after recovery.
- Final C3 Super Mini defaults: B1..B5 GPIO1..5 active-low to GND; EC11 switch GPIO6, A GPIO7, B GPIO10; status LED disabled (`PANEL_STATUS_LED_PIN=-1`). Keep GPIO8/9 free because of C3 boot constraints. Pairing gesture is B1+B3 held 2.5 s; their individual actions are consumed once they overlap. Encoder press cycles Brightness → Speed → Scale; each `uEncButton::turn()` is already one detent—do not restore a multi-callback threshold.
- For temporary field diagnosis, add `-D DEBUG` only to a specific panel device env's `build_flags`. It enables non-secret `ACT` input/queue/TX/ACK telemetry; remove the flag afterward. Runtime marker `PANEL_RADIO_RUNTIME=confirm-clock-order-v1` identifies the current panel build.
- PairAccept handling is asynchronous: retain the lamp broadcast peer until its send callback has been processed in the service tick. Panel runtime must sample time again after draining RX before deadline checks; otherwise peer setup may advance the clock and skip `Confirm`.

## Project conventions

- Code is split by domain under `src/core`, `hardware`, `network`, `notification`, `storage`, `time`, `effect`, `audio`, `text`, `util`. The web UI lives under `src/network/` (there is no separate `src/web/`).
- Local timer helpers `src/util/timer.h` and `src/util/periodic_timer.h` are part of build; do not assume every dependency comes from `lib_deps`.
- `.pio`, `.omo`, `.slim/deepwork/`, `.artifacts/`, generated VS Code files, and `compile_commands.json` are ignored; keep build artifacts out of commits.

## Library dependency sources

- EspConnectivity is supplied by the immutable Git dependency in shared `[env].lib_deps`, replacing the former project-local HaMqttEntities, MqttController, OtaController, and WifiController libraries.
- PlatformIO downloads dependency source trees into `.pio/libdeps/<env>/` (e.g. `.pio/libdeps/lamp1_ota/FastLED/`, `.../Settings/`).
- `.pio` is gitignored, so the directory is not in git but exists locally after the first `pio run -e <env>`. For `lamp1_ota` look under `.pio/libdeps/lamp1_ota/`.
- Before fetching library docs externally, check whether the source is already readable locally under `.pio/libdeps/` and read it there — this matches the exact versions pinned by `platformio.ini` / `lib_deps`.
- The `clonedeps` skill remains the tool for cloning an upstream repo into a separate workspace when `.pio/libdeps/` is insufficient (forks, older snapshots, cross-referencing).
