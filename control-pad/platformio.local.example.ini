; Copy is created by `just panel provision <name>` as platformio.local.ini.
; It is intentionally ignored because each profile contains its private pairing key.
;
; [env:panel1]
; extends = env:profile
; custom_panel_pair_key = 32_HEX_CHARACTER_PAIRING_KEY
; ;build_flags =
; ;  ${env:profile.build_flags}
; ;  -D DEBUG
